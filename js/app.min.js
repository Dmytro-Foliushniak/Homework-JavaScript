const length = prompt("Введіть довжину масиву:");

const myArray = [];

for (let i = 0; i < length; i++) {
    myArray.push(+prompt('Ender element ' + (i + 1)))
    myArray.sort((a,b) => a - b);
    console.log(myArray)
}

myArray.splice(2,3);
alert(myArray)

